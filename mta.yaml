_schema-version: "3.2"
ID: openpobooksrpt
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: openpobooksrpt-destination-content
  type: com.sap.application.content
  requires:
  - name: openpobooksrpt-destination-service
    parameters:
      content-target: true
  - name: openpobooksrpt-repo-host
    parameters:
      service-key:
        name: openpobooksrpt-repo-host-key
  - name: openpobooksrpt-uaa
    parameters:
      service-key:
        name: openpobooksrpt-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: openpobooksrpt_html_repo_host
          ServiceInstanceName: openpobooksrpt-html5-service
          ServiceKeyName: openpobooksrpt-repo-host-key
          sap.cloud.service: openpobooksrpt
        - Authentication: OAuth2UserTokenExchange
          Name: openpobooksrpt_uaa
          ServiceInstanceName: openpobooksrpt-xsuaa-service
          ServiceKeyName: openpobooksrpt-uaa-key
          sap.cloud.service: openpobooksrpt
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: openpobooksrpt-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: openpobooksrpt-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - openpobooksrpt.zip
      name: openpobooksrpt
      target-path: resources/
- name: openpobooksrpt
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: openpobooksrpt-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: openpobooksrpt-destination-service
    service-plan: lite
- name: openpobooksrpt-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: openpobooksrpt-xsuaa-service
    service-plan: application
- name: openpobooksrpt-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: openpobooksrpt-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
